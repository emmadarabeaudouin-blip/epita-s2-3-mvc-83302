@model VgcCollege.Domain.Entities.AssignmentResult

@{
    ViewData["Title"] = "Grade Assignment";
    var assignment = ViewBag.Assignment as VgcCollege.Domain.Entities.Assignment;
    var facultyId = ViewBag.FacultyId;
}

<div class="container mt-4">
    <h1 class="mb-4">Grade Assignment</h1>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <p><strong>Assignment:</strong> @assignment?.Title</p>
            <p><strong>Max Score:</strong> @assignment?.MaxScore</p>
        </div>
    </div>

    <form asp-action="CreateAssignmentResult" method="post">
        @Html.AntiForgeryToken()

        <input type="hidden" asp-for="AssignmentId" />
        <input type="hidden" name="facultyId" value="@facultyId" />

        <div class="mb-3">
            <label asp-for="StudentProfileId" class="form-label">Student</label>
            <select asp-for="StudentProfileId" asp-items="ViewBag.Students" class="form-select">
                <option value="">-- Select Student --</option>
            </select>
            <span asp-validation-for="StudentProfileId" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Score" class="form-label"></label>
            <input asp-for="Score" class="form-control" />
            <span asp-validation-for="Score" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Feedback" class="form-label"></label>
            <textarea asp-for="Feedback" class="form-control"></textarea>
            <span asp-validation-for="Feedback" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary">Save Result</button>
        <a asp-action="Assignments" asp-route-id="@facultyId" class="btn btn-secondary">Cancel</a>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}